# 🎯 КАК НАСТРОИТЬ РАБОТУ БОТА В ОДНОМ ТОПИКЕ

## 📋 ЧТО СДЕЛАНО:

Добавлен фильтр по топику (ветке) в группе Telegram.
Теперь бот может работать только в одной конкретной ветке!

---

## 🔧 КАК НАСТРОИТЬ:

### Шаг 1: Узнай ID топика

**Вариант A: Временно включи логирование**

1. В BotHost.ru добавь переменную окружения:
   ```
   TOPIC_ID=  (оставь пустым)
   ```

2. Перезапусти бота

3. Напиши боту `/start` **в нужном топике**

4. Зайди в логи BotHost.ru

5. Найди строку типа:
   ```
   Сообщение из топика 12345
   ```
   
6. Скопируй это число (например `12345`)

**Вариант B: Используй другого бота**

1. Добавь в группу бота @JsonDumpBot или @getidsbot
2. Отправь ему любое сообщение **в нужном топике**
3. Он покажет `message_thread_id` - это и есть ID топика

**Вариант C: Через Bot API (для продвинутых)**

1. Отправь боту сообщение в нужном топике
2. Проверь через API: `getUpdates`
3. Найди поле `message_thread_id`

---

### Шаг 2: Добавь ID топика в BotHost.ru

1. Зайди в панель BotHost.ru
2. Открой свой проект
3. Перейди в **"Переменные окружения"**
4. Добавь новую переменную:
   ```
   Название: TOPIC_ID
   Значение: 12345  (твой ID топика)
   ```
5. Сохрани
6. Перезапусти бота

---

### Шаг 3: Проверь

1. Напиши боту `/start` **в нужном топике** → должен ответить ✅
2. Напиши боту `/start` **в другом топике** → НЕ должен ответить ✅
3. Напиши боту `/start` **в основной ветке** → НЕ должен ответить ✅

---

## ⚙️ ДОПОЛНИТЕЛЬНЫЕ НАСТРОЙКИ:

### Работать везде (отключить фильтр):
Просто удали переменную `TOPIC_ID` или оставь её пустой:
```
TOPIC_ID=
```

### Работать только в основной ветке (General):
```
TOPIC_ID=1
```
(обычно основная ветка имеет ID = 1, но проверь в логах)

---

## 🐛 РЕШЕНИЕ ПРОБЛЕМ:

### Бот не отвечает нигде:
- Проверь что `TOPIC_ID` правильный
- Убедись что бот в группе
- Проверь логи на ошибки

### Бот отвечает везде:
- Проверь что переменная `TOPIC_ID` добавлена
- Убедись что бот перезапущен после добавления
- Проверь логи: должно быть "Бот будет работать только в топике ID: ..."

### Callback кнопки не работают:
- Это нормально! Callback всегда обрабатываются
- Фильтр действует только на текстовые сообщения
- Если нажал кнопку в нужном топике - она сработает везде

---

## 📊 ПРИМЕР НАСТРОЙКИ:

```
# В BotHost.ru переменные окружения:

BOT_TOKEN=123456789:ABCdefGHI...
DB_PATH=/data/chicken.db
ADMIN_IDS=123456789
TOPIC_ID=54321           ← ID топика "Учёт курицы"
LOW_THRESHOLD=300
```

Теперь бот работает только в топике с ID `54321`!

---

## ✅ ГОТОВО!

Бот теперь работает только в одном топике!

Если нужна помощь - пиши! 🚀
