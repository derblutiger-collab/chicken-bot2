# 🚀 Инструкции по развёртыванию

## Вариант 1: Обычный запуск на сервере

### 1. Подготовка сервера
```bash
# Обновление системы
sudo apt update && sudo apt upgrade -y

# Установка Python и необходимых пакетов
sudo apt install python3 python3-pip python3-venv git -y
```

### 2. Клонирование и настройка
```bash
# Клонирование репозитория
git clone <your-repo-url>
cd chicken_bot

# Создание виртуального окружения
python3 -m venv venv
source venv/bin/activate

# Установка зависимостей
pip install -r requirements.txt

# Настройка переменных окружения
cp .env.example .env
nano .env  # Отредактировать и добавить BOT_TOKEN
```

### 3. Запуск бота
```bash
# Простой запуск (для тестирования)
python main.py

# Запуск в фоне с nohup
nohup python main.py > bot.log 2>&1 &

# Или с использованием screen
screen -S chicken_bot
python main.py
# Ctrl+A, D для отсоединения
```

### 4. Остановка бота
```bash
# Найти процесс
ps aux | grep main.py

# Убить процесс
kill <PID>

# Или если использовали screen
screen -r chicken_bot
# Ctrl+C для остановки
```

## Вариант 2: Запуск через systemd (рекомендуется)

### 1. Настройка service файла
```bash
# Отредактировать файл chicken_bot.service
nano chicken_bot.service

# Заменить:
# - YOUR_USER на ваше имя пользователя
# - /path/to/chicken_bot на полный путь к проекту
```

### 2. Установка и запуск сервиса
```bash
# Скопировать service файл
sudo cp chicken_bot.service /etc/systemd/system/

# Перезагрузить systemd
sudo systemctl daemon-reload

# Включить автозапуск
sudo systemctl enable chicken_bot

# Запустить сервис
sudo systemctl start chicken_bot

# Проверить статус
sudo systemctl status chicken_bot
```

### 3. Управление сервисом
```bash
# Остановить
sudo systemctl stop chicken_bot

# Перезапустить
sudo systemctl restart chicken_bot

# Посмотреть логи
sudo journalctl -u chicken_bot -f

# Посмотреть последние 100 строк логов
sudo journalctl -u chicken_bot -n 100
```

## Вариант 3: Docker (самый простой)

### 1. Установка Docker
```bash
# Установка Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# Добавление пользователя в группу docker
sudo usermod -aG docker $USER
newgrp docker

# Установка Docker Compose
sudo apt install docker-compose -y
```

### 2. Настройка и запуск
```bash
# Клонирование репозитория
git clone <your-repo-url>
cd chicken_bot

# Настройка .env
cp .env.example .env
nano .env  # Добавить BOT_TOKEN

# Создание директории для БД
mkdir -p data

# Сборка и запуск
docker-compose up -d

# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down
```

### 3. Обновление бота
```bash
# Остановить контейнер
docker-compose down

# Получить обновления
git pull

# Пересобрать и запустить
docker-compose up -d --build
```

## Вариант 4: Docker без docker-compose

```bash
# Сборка образа
docker build -t chicken_bot .

# Запуск контейнера
docker run -d \
  --name chicken_bot \
  --restart unless-stopped \
  -v $(pwd)/data:/data \
  --env-file .env \
  chicken_bot

# Просмотр логов
docker logs -f chicken_bot

# Остановка и удаление
docker stop chicken_bot
docker rm chicken_bot
```

## 🔧 Получение Telegram ID

Чтобы узнать ваш Telegram ID для настройки ADMIN_IDS:

1. Напишите боту [@userinfobot](https://t.me/userinfobot)
2. Скопируйте ваш ID
3. Добавьте в .env: `ADMIN_IDS=ваш_id`

## 🔐 Безопасность

### Защита .env файла
```bash
# Установить правильные права доступа
chmod 600 .env
```

### Настройка файрвола (опционально)
```bash
# Разрешить только SSH
sudo ufw allow ssh
sudo ufw enable
```

## 📊 Мониторинг

### Просмотр логов в реальном времени
```bash
# systemd
sudo journalctl -u chicken_bot -f

# Docker
docker-compose logs -f

# Обычный запуск
tail -f bot.log
```

### Проверка статуса
```bash
# systemd
sudo systemctl status chicken_bot

# Docker
docker-compose ps

# Обычный процесс
ps aux | grep main.py
```

## 🔄 Резервное копирование

### Бэкап базы данных
```bash
# Создание бэкапа
cp chicken.db chicken.db.backup.$(date +%Y%m%d_%H%M%S)

# Автоматический бэкап (добавить в crontab)
crontab -e
# Добавить строку (бэкап каждый день в 3:00):
0 3 * * * cp /path/to/chicken_bot/chicken.db /path/to/backups/chicken.db.$(date +\%Y\%m\%d)
```

### Восстановление из бэкапа
```bash
# Остановить бота
sudo systemctl stop chicken_bot  # или docker-compose down

# Восстановить БД
cp chicken.db.backup.20240101_120000 chicken.db

# Запустить бота
sudo systemctl start chicken_bot  # или docker-compose up -d
```

## 🆘 Решение проблем

### Бот не запускается
```bash
# Проверить логи
sudo journalctl -u chicken_bot -n 50

# Проверить переменные окружения
cat .env

# Проверить права на файлы
ls -la

# Проверить зависимости
pip list
```

### Бот теряет соединение
```bash
# Перезапустить
sudo systemctl restart chicken_bot

# Проверить интернет
ping telegram.org
```

### База данных повреждена
```bash
# Восстановить из бэкапа
cp chicken.db.backup chicken.db

# Или удалить и создать заново
rm chicken.db
python main.py  # БД создастся автоматически
```

## 📝 Обновление бота

### Через Git
```bash
# Остановить бота
sudo systemctl stop chicken_bot

# Получить обновления
git pull

# Обновить зависимости (если изменились)
source venv/bin/activate
pip install -r requirements.txt --upgrade

# Запустить бота
sudo systemctl start chicken_bot
```

### Через Docker
```bash
# Остановить
docker-compose down

# Обновить
git pull

# Пересобрать и запустить
docker-compose up -d --build
```

## 🎯 Проверка работоспособности

После развёртывания проверьте:

1. ✅ Бот отвечает на /start
2. ✅ Можно создать партию
3. ✅ Можно взять порцию
4. ✅ История работает
5. ✅ Админ-команды доступны (если настроен ADMIN_IDS)
6. ✅ Логи пишутся корректно

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи
2. Убедитесь что BOT_TOKEN правильный
3. Проверьте интернет-соединение
4. Создайте issue в репозитории
