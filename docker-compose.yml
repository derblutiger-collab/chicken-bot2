version: '3.8'

services:
  chicken_bot:
    build: .
    container_name: chicken_bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/data
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
