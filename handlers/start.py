"""
Обработчик команды /start и общих команд
"""
from aiogram import Router, F
from aiogram.filters import Command
from aiogram.types import Message, CallbackQuery
from aiogram.fsm.context import FSMContext

from keyboards import main_kb, back_kb


router = Router(name="start")


@router.message(Command("start"))
async def start_command(message: Message, state: FSMContext):
    """Команда /start"""
    await state.clear()
    await message.answer(
        "🐔 <b>Привет! Я помогу отслеживать курочку!</b>\n\n"
        "📝 Создай новую партию, укажи сколько было <b>сырой</b> и сколько получилось <b>готовой</b>.\n"
        "🍗 Я буду автоматически пересчитывать остатки и напоминать, когда пора готовить новую.\n\n"
        "Работаем красиво! 😌",
        reply_markup=main_kb()
    )


@router.callback_query(F.data == "cancel")
async def cancel_operation(callback: CallbackQuery, state: FSMContext):
    """Отмена текущей операции"""
    await state.clear()
    await callback.message.edit_text(
        "❌ Операция отменена",
        reply_markup=main_kb()
    )
    await callback.answer("Отменено")


@router.callback_query(F.data == "help")
async def help_command(callback: CallbackQuery):
    """Помощь по использованию бота"""
    help_text = (
        "<b>📖 Как пользоваться ботом:</b>\n\n"
        
        "<b>1️⃣ Новая партия</b>\n"
        "Когда приготовишь курицу:\n"
        "• Нажми «➕ Новая партия»\n"
        "• Введи вес <b>сырой</b> курицы (например: 1500 или 1.5кг)\n"
        "• Введи вес <b>готовой</b> курицы\n"
        "• Я запомню коэффициент и буду считать остатки\n\n"
        
        "<b>2️⃣ Взять порцию</b>\n"
        "Когда берёшь курицу:\n"
        "• Нажми «🍗 Взять порцию»\n"
        "• Выбери вес или введи свой\n"
        "• Я покажу сколько это готовой и обновлю остаток\n\n"
        
        "<b>3️⃣ Проверить остаток</b>\n"
        "• Нажми «🥩 Остаток»\n"
        "• Я покажу сколько сырой осталось и сколько это готовой\n\n"
        
        "<b>4️⃣ История</b>\n"
        "• Нажми «📜 История»\n"
        "• Посмотри последние операции\n\n"
        
        "💡 <b>Форматы ввода веса:</b>\n"
        "• 1500 или 1500г - граммы\n"
        "• 1.5 или 1.5кг - килограммы\n"
        "• полкило, четверть и т.д.\n\n"
        
        "⚠️ Когда остаток станет меньше 300г, я напомню приготовить новую партию!\n\n"
        
        "Приятного аппетита! 🍗"
    )
    
    await callback.message.edit_text(help_text, reply_markup=back_kb())
    await callback.answer()
