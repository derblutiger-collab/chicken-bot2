# 🐔 Chicken Bot - Бот для отслеживания курицы

Telegram-бот для учёта сырой и готовой курицы с автоматическим пересчётом порций.

## 📋 Возможности

- ✅ Учёт партий курицы (сырая → готовая)
- ✅ Автоматический расчёт коэффициента уменьшения веса
- ✅ Быстрое взятие порций с пересчётом остатков
- ✅ История всех операций
- ✅ Напоминания о низком остатке
- ✅ Админ-панель для управления
- ✅ Умный парсинг веса (1500, 1.5кг, полкило и т.д.)
- ✅ Автоудаление старых сообщений

## 🚀 Установка

### 1. Клонирование репозитория
```bash
git clone <your-repo-url>
cd chicken_bot
```

### 2. Создание виртуального окружения
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

### 3. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 4. Настройка переменных окружения
```bash
cp .env.example .env
```

Отредактируйте файл `.env` и укажите ваш токен бота:
```env
BOT_TOKEN=your_bot_token_here
ADMIN_IDS=123456789  # Ваш Telegram ID
```

### 5. Запуск бота
```bash
python main.py
```

## 📁 Структура проекта

```
chicken_bot/
├── handlers/           # Обработчики команд и callback'ов
│   ├── __init__.py    # Регистрация обработчиков
│   ├── start.py       # Команда /start и помощь
│   ├── status.py      # Просмотр остатка
│   ├── batch.py       # Создание новой партии
│   ├── take.py        # Взятие порции
│   ├── history.py     # Просмотр истории
│   ├── admin.py       # Админ-команды
│   └── common.py      # Общие утилиты
├── utils/             # Вспомогательные утилиты
│   ├── __init__.py
│   ├── parser.py      # Парсинг веса
│   └── validators.py  # Валидация данных
├── config.py          # Конфигурация
├── database.py        # Работа с БД
├── keyboards.py       # Клавиатуры
├── middlewares.py     # Middleware
├── states.py          # FSM состояния
├── main.py           # Точка входа
├── requirements.txt   # Зависимости
├── .env.example      # Пример конфигурации
├── .gitignore        # Git ignore
└── README.md         # Документация
```

## 🎯 Использование

### Основные команды
- `/start` - Запуск бота
- `/admin` - Админ-панель (только для администраторов)
- `/reset` - Полный сброс данных (только для администраторов)
- `/stats` - Статистика (только для администраторов)

### Создание новой партии
1. Нажмите "➕ Новая партия"
2. Введите вес сырой курицы (например: 1500 или 1.5кг)
3. Введите вес готовой курицы
4. Бот автоматически рассчитает коэффициент

### Взятие порции
1. Нажмите "🍗 Взять порцию"
2. Выберите готовый вес или введите свой
3. Бот пересчитает остатки и покажет вес готовой порции

### Форматы ввода веса
- `1500` или `1500г` - граммы
- `1.5` или `1.5кг` - килограммы  
- `полкило`, `четверть` - словами
- `1/2`, `1/4` - дроби

## ⚙️ Конфигурация

Все настройки задаются через переменные окружения в файле `.env`:

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `BOT_TOKEN` | Токен Telegram бота | **Обязательно** |
| `DB_PATH` | Путь к базе данных | `chicken.db` |
| `LOW_THRESHOLD` | Порог низкого остатка (г) | `300` |
| `MAX_MESSAGES` | Макс. сохраняемых сообщений | `5` |
| `ADMIN_IDS` | ID администраторов (через запятую) | - |
| `MIN_WEIGHT` | Минимальный вес (г) | `10.0` |
| `MAX_WEIGHT` | Максимальный вес (г) | `10000.0` |

## 🛡️ Безопасность

- Все пользовательские данные валидируются
- Защита от SQL-инъекций через параметризованные запросы
- Обработка всех исключений
- Логирование всех операций
- Права администратора для критичных операций

## 📊 База данных

Бот использует SQLite с тремя таблицами:

- `batch` - текущая партия курицы
- `history` - история операций
- `messages` - сообщения для автоудаления

## 🔧 Разработка

### Добавление новых обработчиков

1. Создайте файл в `handlers/`
2. Создайте роутер:
```python
from aiogram import Router
router = Router(name="my_handler")
```

3. Зарегистрируйте в `handlers/__init__.py`:
```python
from . import my_handler

def register_handlers(dp: Dispatcher):
    # ...
    dp.include_router(my_handler.router)
```

### Запуск с отладкой
```bash
# Установить уровень логирования
export LOG_LEVEL=DEBUG
python main.py
```

## 📝 Лицензия

MIT License

## 👤 Автор

Создано с ❤️ для удобного учёта курочки!

## 🤝 Вклад

Pull requests приветствуются! Для больших изменений сначала откройте issue для обсуждения.

## 📮 Поддержка

Если у вас возникли вопросы или проблемы, создайте issue в репозитории.
